# This is the self-used common block designed by Lingxuan Wang

## Environment

Use your conda environment for development and installs. From PowerShell:

```powershell
conda activate <your-env>
python setup.py bdist_wheel
```

The project no longer relies on a repository-local `.venv`.
